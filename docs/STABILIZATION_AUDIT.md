# DatumDock 已实现功能稳定化审计

> 审计范围：内部资料库、受管数据集、图片池、标签、矩形标注、双状态复核、快捷键、X-AnyLabeling/LabelMe 互操作和当前正式 GUI。模型推理、YOLO 导出、备份、跨数据集治理和安装包不属于本轮已实现范围。

## 1. 缺陷账本

| 等级 | 缺陷 | 根因 | 修复与证据 | 状态 |
| --- | --- | --- | --- | --- |
| P1 | X-AnyLabeling 4.x 四点矩形导入后不可编辑 | LabelMe 解析器只接受两个对角点 | 两点/四点共用分类函数；完整轴对齐四角规范化为内部矩形；预检、提交、框数和反向标签索引使用同一结论 | 已关闭 |
| P1 | 双击改派后出现版本冲突 | 双击前的移动快照在模态窗口关闭后的 `mouseRelease` 中再次提交 | 双击立即清理拖动事务并吞掉配对释放；移动/缩放只比较几何坐标；真实回归断言一次改派只有一个版本和一个保存请求 | 已关闭 |
| P1 | 改派成功后继续画框仍可能发生基准版本冲突 | 编辑前启动的同图后台加载在保存后迟到，把工作台内存文档从版本 1 覆盖回版本 0 | 有效编辑立即使旧加载代号失效；低版本加载不得覆盖较新内存文档；回归强制注入迟到版本并验证 `test1 → test2 → 下一框 test2` 两次保存和重启后的两个矩形 | 已关闭 |
| P2 | 保存失败提示笼统且没有详情入口 | UI 丢弃了结构化异常上下文 | 保存失败记录请求 ID、版本、摘要、标签修订、异常链和恢复状态；状态栏可打开详情并按可重试性提供操作 | 已关闭 |
| P2 | 旋转四点 rectangle 警告会阻断整张图片导入 | 警告被错误写入 `blocking_issues` | 只有错误级问题阻断；旋转/退化四点内容随图片只读保留 | 已关闭 |
| P2 | A0.11 图标只存在资源、未覆盖正式入口 | 原测试只验证 SVG 文件可渲染 | 首页、工作台、数据集卡片、标签/图片治理和三种删除语义完成绑定；注册表可枚举并报告缺失资源 | 已关闭 |
| P3 | Pillow `getdata()` 产生弃用警告 | 旧感知哈希代码继续调用即将移除的接口 | 改用 `get_flattened_data()`；DatumDock 自身弃用警告清零 | 已关闭 |
| P3 | 英文复核和排序控件在原生截图中裁切 | 空复核值被显示成长文案，两个高频动作缺少最小宽度，排序标签过长 | 空值改为中性 `—`，动作收敛为 `Complete / Delete Box` 并设置最小宽度，排序使用 `A–Z / Z–A`；原生中英文截图复验通过 | 已关闭 |

## 2. 功能证据矩阵

| 范围 | 真实入口 | 自动化证据 | 外部/人工证据 | 当前判定 |
| --- | --- | --- | --- | --- |
| 资料库与数据集 | 普通模式主页和数据集卡片 | 初始化、重启恢复、切换、改名、归档、模板、永久删除与故障恢复 | 临时资料库 GUI 启动 | 已接入 |
| 图片池 | 工作台导入和图片治理 | 六类格式、转码、重复、相似、分页、重命名、回收站和永久删除 | 万级索引使用分页模型 | 已接入 |
| 标签与矩形 | 标签页、工作台和快速标签窗口 | 标签迁移、两点/四点矩形、拖拽/两点创建、改派、撤销/重做、自动保存和复核 | 实机缺陷已转为回归 | 已接入 |
| X-AnyLabeling | 正式导入、修复和导出向导 | 路径安全、标签新建、字段保序、两点/四点、100 图闭环和原子回滚 | v3.3.10 实际 GUI 打开/编辑/回导仍受可信依赖 TLS 阻塞 | 实现完成，外部硬门槛受阻 |
| 图标与双语 UI | 当前正式页面 | 资源渲染、主界面绑定、翻译键一致性、分辨率和 DPI 回归 | 安装快捷方式图标属于后续阶段 | 源码界面已接入 |
| 未来入口 | 模型、YOLO、备份等 | 验证明确提示未接入且不产生文件副作用 | 无 | 未实现 |

## 3. 当前质量门

- Python 3.11、Ruff、Ruff 格式检查和 `compileall` 是本轮硬门槛。
- 测试清单当前为 248 项；最终代码的两次连续完整结果分别为 `247 passed、1 skipped in 78.25s` 和 `247 passed、1 skipped in 78.19s`，均无 DatumDock 警告。
- Windows 符号链接安全用例因当前账户缺少权限可在本机跳过，但已加入 Windows CI；在具备权限的运行器上不得跳过。
- `pytest-cov` 已加入开发依赖和 Windows CI。当前本机从可信 PyPI 安装时发生 TLS EOF，未关闭证书校验；恢复后必须生成分支覆盖率报告，并按核心文件事务/标注/互操作模块 90%、其他当前生产模块 85% 的目标继续补测试。
- 全部自动化测试使用临时绝对 `DATUMDOCK_DATA_DIR`；预览模式必须零写盘，外部来源保持只读。
- 普通模式和 `--ui-preview` 均在 Windows 原生 Qt 后端进入事件循环；普通临时资料库生成 2 个初始化文件，预览模式生成 0 个文件。
- 本轮原生视觉证据位于忽略提交的 `build/ui-review/stabilization-native/`：步骤五双语三分辨率 24 张，近期整改双语页面 6 张；抽查确认中文字体、英文长文案、语义图标和危险删除对话框无本轮已知裁切。
- 真实 `%LOCALAPPDATA%\DatumDock` 仅做只读采样；最终前后均为 23 个文件，按“相对路径 + 制表符 + 文件 SHA-256”生成的树摘要均为 `BEFFB245757702C2C0659E6CBD443A8D0BB62A7A25CA091B30F6DD8293B2B0FF`。

## 4. 不得越界的完成声明

本轮只能声明截至步骤五已经接入的能力完成稳定化修复。以下能力仍是后续阶段：

- ONNX/PT 模型管理与真实推理；
- YOLO Detection 训练数据集导出；
- 数据集备份导入导出；
- 跨数据集复制、移动和合并；
- PyInstaller/Inno Setup 安装包及干净系统验收。

X-AnyLabeling v3.3.10 的第三方 GUI 硬门槛未取得证据前，不宣称完整步骤五或整体 L2 已验收完成。

## 5. 自检评分与阻塞

| 领域 | 得分 | 说明 |
| --- | ---: | --- |
| 需求覆盖 | 24 / 25 | 当前已接入范围完成整改；未来功能继续明确未接入。 |
| 数据完整性与事务安全 | 25 / 25 | 文件/SQLite 恢复协议、迟到加载保护和真实资料库隔离均有回归证据。 |
| X-AnyLabeling 兼容性 | 12 / 15 | 两点/四点格式和目录闭环通过；v3.3.10 外部 GUI 硬门槛仍受阻。 |
| GUI 与使用体验 | 10 / 10 | 原生双语截图、语义图标、保存诊断和英文裁切整改通过。 |
| 测试与稳定性 | 13 / 15 | 两轮完整回归通过；本机覆盖率报告和有符号链接权限的 CI 尚未取得。 |
| 文档与工程质量 | 10 / 10 | 当前事实、历史证据、未来边界和阻塞已统一。 |
| **合计** | **94 / 100** | 分数超过 90，但外部硬门槛不能由评分抵消。 |

当前仍有两个外部恢复条件：

1. 可信依赖源恢复后，在独立 Python 3.11 环境完成 X-AnyLabeling v3.3.10 打开、编辑、保存和回导。
2. 可信 PyPI、企业镜像或离线 wheel 可用后安装 `pytest-cov`，生成分支覆盖率；随后在有符号链接权限的 Windows CI 运行完整质量门。

因此本轮可以判定“当前已接入功能的自动化稳定化整改通过”，但不能判定“完整步骤五/L2 外部验收通过”，也不满足按计划自动推送 `main` 的全部硬条件。

## English Summary

This audit covers only the managed features implemented through Step 5. It records fixes for X-AnyLabeling four-point rectangles, duplicate label-change autosaves, actionable save diagnostics, semantic icon bindings, and warning cleanup. The fixed X-AnyLabeling v3.3.10 external GUI gate and local branch-coverage report remain blocked by trusted dependency TLS failures; model inference, YOLO export, backup, cross-dataset governance, and installer delivery remain future work.
