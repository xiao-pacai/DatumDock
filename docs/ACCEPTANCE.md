# DatumDock MVP 验收标准

所有条目都必须可以实际操作或由自动化测试验证。

当前对标等级和对应质量闸门见 `docs/X_ANYLABELING_BASELINE.md`。除本文件的功能验收外，声明达到 L1、L2 或 L3 时必须满足该基线中对应等级的全部必需项。

最新入口与存储验收以 [内部数据集主页与存档式管理方案](DATASET_LIBRARY.md) 为准。以下 A0 为下一轮实现的最高优先级闸门；在 A0 全部通过前，不得把旧的工作区 / 项目入口描述为最终产品体验。

## A0. 内部数据集主页与存档式入口（分步实施）

- [ ] 首次启动不弹出工作区或项目目录选择器，并自动建立当前用户的 DatumDock 内部资料库。
- [ ] 每次正常启动均先显示带完整 Logo、已有数据集卡片和“新建数据集”入口的主页。
- [ ] 主页可按名称搜索数据集，并可按最后修改时间、创建时间或名称排序。
- [ ] 数据集卡片显示名称、代表缩略图、图片数量、标签数量、复核进度和最后修改时间。
- [ ] 首次启动主页显示“创建数据集 → 配置标签 → 导入图片 → 标注/审核 → 导出训练数据”五步快速开始；可跳过、折叠并从学习中心重新打开。
- [ ] 首页学习中心至少提供 DatumDock 入门、标签与矩形框、AI 审核、YOLO Detection 入门、数据划分/数据泄露、导出与训练准备、X-AnyLabeling 互操作、备份恢复和常见问题入口。
- [ ] 已有数据集时，数据集卡片仍是主页主内容；教程不会以强制弹窗、自动播放或大幅轮播阻挡打开数据集。
- [ ] 核心教程在断网环境中可打开，正文、目录、必要插图、上一步/下一步和完成标记可用。
- [ ] 教程可在简体中文与英文间即时切换，并保持同一章节、滚动位置和完成进度。
- [ ] YOLO 教程正确解释标签行、归一化坐标、`data.yaml`、数据划分、负样本和数据泄露，并明确 MVP 只导出数据集、不直接训练模型。
- [ ] 第三方训练命令和外部官方文档带适用版本或外链标记；外部链接失败不影响本地教程和主页。
- [ ] 教程中的功能跳转不会绕过导入、删除、模型推理或导出的原有确认与校验。
- [ ] 教程进度保存在全局设置中，不进入数据集、数据集备份、YOLO 导出或 X-AnyLabeling 目录；升级后旧进度保留。
- [ ] 点击已有数据集卡片直接进入该数据集的标注与管理工作台。
- [ ] 新建空数据集时不要求选择保存目录；创建成功后直接进入同一个空标注工作台，并提供“导入图片”和“管理标签”引导。
- [ ] 从其他数据集复制配置时可复用标签集、命名与导入规则，但默认不复制图片或标注。
- [ ] 从工作台返回主页不会修改或重新导入数据；自动保存失败时必须先处理，不能静默丢失修改。
- [ ] 重启应用后，数据集卡片、图片、标签、标注、复核状态和排序摘要均正确恢复。
- [ ] 中文和英文界面均不再显示“打开工作区”“选择项目根目录”或用户可见项目树等旧主流程。
- [ ] 所有受管内容位于当前 Windows 用户的应用数据目录，而非安装目录；升级保留资料库，卸载默认不删除用户数据。
- [ ] 导入外部图片会复制并转为受管 PNG；删除 DatumDock 样本不会改写或删除外部来源文件。
- [ ] 创建两个数据集后，图片、标签、标注、模型、索引、复核状态、回收站与缓存均不串用。
- [ ] 单个数据集元数据损坏不会导致主页或其他数据集无法使用，并提供明确诊断或恢复信息。
- [ ] 如存在旧 `Workspace -> Project -> Dataset` 数据，迁移可预览、可校验、失败可回退且不会自动删除来源。
- [x] 数据集卡片“更多”菜单提供整数据集永久删除；卡片主体点击、打开和切换操作不会误触删除。
- [x] 永久删除前显示图片、标注、标签、模型、索引、缩略图、缓存、样本回收站、文件数和占用空间；统计失败时禁止删除。
- [x] 用户必须输入完整数据集名称并完成第二次明确确认；任一步取消、关闭或按 `Esc` 均不修改资料库。
- [x] 保存失败的内存标注、未完成恢复操作或写入型后台任务会阻止删除，且应用不会自动放弃标注或强制中断原子步骤。
- [x] 删除只作用于资料库登记的目标 UUID 目录和登记信息；外部来源、X-AnyLabeling/YOLO 导出目录、备份包和其他数据集的文件树哈希保持不变。
- [x] 在目录暂存、资料库登记移除、最终清理和启动恢复各阶段注入失败后，不会留下仍登记但无法打开的半删除卡片；不能证明一致时保留诊断现场且不报告成功。
- [x] 归档仍可恢复且不删除内容；首版不提供整数据集回收站，样本级回收站阈值不会影响整数据集永久删除行为。

### A0.1 步骤二验收记录（2026-07-19）

- [x] 普通模式首次启动在 `%LOCALAPPDATA%\DatumDock` 初始化空资料库，不显示目录选择器。
- [x] `DATUMDOCK_DATA_DIR` 只作为测试/开发绝对路径覆盖，自动化测试不污染真实用户目录。
- [x] 主页真实创建空数据集，创建完成后直接进入带 0 张图片、空画布和导入提示的工作台。
- [x] 重启 Service 后仍能恢复数据集卡片和配置；点击卡片及顶部下拉可打开和切换两个真实数据集。
- [x] 活动名称经过首尾空格清理和大小写不敏感判重；非法字符、保留名称和路径逃逸被拒绝。
- [x] 重命名不改变 UUID 或目录；归档不删除内容，主页可筛选并恢复。
- [x] 从其他数据集复制标签、命名、显示、导入和默认划分配置，并确保源目标对象相互独立。
- [x] 配置复制不复制图片、标注、缩略图、模型、回收站或导出记录。
- [x] `library.json` 与数据集 JSON 使用同目录临时文件、刷新和原子替换；创建登记失败不会留下活动半成品。
- [x] 损坏资料库索引不会被覆盖；单个损坏数据集显示诊断卡片且不阻止健康数据集打开。
- [x] `library.json` 缺失、有效 UUID 目录未登记或主页摘要过期时，启动以有效 `dataset.json` 自动恢复登记；损坏孤儿保留为诊断卡片。
- [x] 非 UUID 目录和符号链接只报告、不跟随、不删除、不移动；当前 Windows 账户无符号链接创建权限，因此自动符号链接用例明确跳过，未知目录保留用例通过。
- [x] 元数据写盘、资料库写盘、回滚和恢复区转移失败均转换为业务错误；`ManagedDatasetGateway.dispatch()` 不向 Qt 抛出裸磁盘异常。
- [x] 标签、标签集稳定 ID 使用规范 UUID；类别 ID 全局唯一，活动训练名和颜色唯一，复核数量不得超过图片数量；Repository 写盘前重新验证可变模型。
- [x] 普通模式损坏诊断为单页只读界面，只显示真实名称、UUID 和原因，不含步骤一演示统计，关闭时不写入资料库。
- [x] 普通模式不显示演示数据或预览横幅；未接入入口只提示且不产生文件副作用。
- [x] `--ui-preview` 不读取或修改真实资料库，关闭后内存修改丢失。
- [x] 中英文切换不改变数据集名称、描述或资料库文件。
- [x] 1366×768、1440×900、1920×1080 的中英文主页和空工作台已完成原生截图检查。
- [x] 独立 Python 3.11 环境下 Ruff、格式、`compileall`、pytest-qt 和完整测试通过：88 passed、1 skipped；普通/预览 Qt 事件循环启动通过，真实用户资料库前后哈希一致。

步骤二当时未勾选的图片池条目已由下方步骤三记录覆盖，标签与标注由步骤四记录覆盖；模型、导出、备份和安装包仍需后续阶段真实实现。

### A0.2 步骤三验收记录（2026-07-19）

- [x] 可从文件或递归目录导入 JPG/JPEG、PNG、BMP、WebP、TIFF 静态图，不跟随符号链接；多帧与损坏图进入失败报告。
- [x] 所有来源经 EXIF 校正、颜色模式规范化和二次打开校验后写为受管 PNG；外部文件树哈希前后不变。
- [x] 完全重复以规范化像素 SHA-256 判定，已有/同批次重复均要求用户逐项跳过或保留。
- [x] 近似候选使用分桶 dHash、Hamming 和颜色距离，初始只为待确认；确认/忽略可重启恢复，不自动删除样本。
- [x] `index.sqlite` 原子迁移到 v1，保留可安全转换的 v0 记录；不安全路径只诊断、不删文件。
- [x] 工作台使用每页 200 条的虚拟模型，支持列表/网格、搜索、状态筛选、排序、翻页、真实缩略图与当前 PNG 画布。
- [x] 画布支持适合窗口、按钮/滚轮缩放、平移和序号/尺寸状态；普通模式不伪造可保存标注。
- [x] 批量重命名先预览冲突，使用两段文件名解决交换，样本 UUID/内容哈希/缩略图键不变。
- [x] 删除阈值按本次数量判定并持久化；回收站恢复冲突时安全改名，永久删除经过两步确认且只触碰受管内容。
- [x] 导入、重命名、回收站、恢复与永久删除使用可恢复操作日志；断电窗口回归验证索引与文件一致。
- [x] 后台任务不依赖 Qt，有稳定 ID、进度、取消令牌、错误和部分结果；关闭时等待当前原子步骤和 Qt 读取任务释放文件句柄。
- [x] 10,000 条合成索引分页、搜索、状态和排序通过，当前页最多 200 个领域对象，不创建 10,000 个 QWidget。
- [x] Python 3.11 完整回归为 127 passed、1 skipped；20 张原生 Windows 截图覆盖中英文、三种分辨率与图片池核心页面。

步骤三当时未勾选的标签与标注条目已由下方步骤四记录覆盖。X-AnyLabeling 完整目录导入导出、模型、YOLO 导出、备份和安装包仍未完成。

### A0.3 步骤四验收记录（2026-07-19）

- [x] 数据集级 `LabelSet` 使用稳定 UUID、修订号和时间；类别 ID 全集合唯一，活动训练名大小写不敏感唯一，活动颜色唯一。
- [x] 标签管理页可新增、编辑显示字段、归档/恢复和搜索；别名、描述、同义词或颜色变化不会改写任何标注 JSON。
- [x] 英文训练名变化先显示图片/框影响并以备份迁移相关 JSON；类别 ID 变化不改写 JSON；中断可由有限恢复目录回滚。
- [x] SQLite 原子迁移到 v2，维护标注摘要、版本、框数、复核状态及 `sample_labels(sample_id, label_id, shape_id)`。
- [x] LabelMe 读写保持矩形与未支持 shape 原顺序，保留未知根字段、`imageData` 与兼容载荷；外部负载递归剔除 `datumdock_` 私有字段。
- [x] 正式画布支持矩形创建、点击选择、框内移动、八点缩放、删除、标签改派、撤销/重做、滚轮缩放、平移和适配窗口。
- [x] 矩形以原图像素坐标保存；零面积、非有限和越界坐标被拒绝，面积大于零的极小框保存时产生质量警告。
- [x] 新建、移动、缩放、删除、换标签、撤销、重做和复核变化立即排入串行自动保存；保存版本和磁盘摘要按 sample UUID 隔离。
- [x] JSON 临时文件验证、原子替换、重读摘要和 SQLite 更新使用恢复标记协调；JSON 已提交但索引失败可在启动后有限回放。
- [x] 损坏 JSON 原字节保持不变并以异常只读显示；未知标签、缺图和待恢复操作不会被普通编辑覆盖。
- [x] 图片级状态持久化为未复核、待审核、已完成、已完成（无目标）和有问题；异常由健康/标注状态派生。
- [x] 已完成要求至少一个框；已完成（无目标）要求零框；删除最后一个框回到待审核，有问题图片编辑后仍保持有问题。
- [x] 标签检查页从 SQLite 分页提取包含目标标签的图片，并可通过结构化导航目标跨页定位和高亮首个相关框。
- [x] 图片列表按需显示真实框数、状态和标签筛选；缩略图标注预览仅在视图请求时加载，不扫描全部 JSON。
- [x] 重命名、回收站恢复和永久删除同步维护标注文件名、`imagePath`、摘要与反向索引；失败回滚恢复原 JSON 字节与摘要。
- [x] 100 张图片连续保存/重开、10,000 条标签/状态分页定位以及两个数据集快速自动保存隔离通过。
- [x] pytest-qt 使用真实鼠标覆盖画框、移动/八点缩放、换标签、删除、撤销/重做和复核。
- [x] Python 3.11 完整回归为 153 passed、1 skipped；22 张原生 Windows 截图覆盖双语、三种分辨率、标签管理、标签检查及保存失败保护。

步骤四不宣称模型自动标注、YOLO 导出、完整 X-AnyLabeling 目录互操作、备份、跨数据集转移或安装包完成。

### A0.4 标注交互整改已验收

> 本节于 2026-07-19 通过实现与复验。A0.3 的五状态条目仅保留历史证据；当前产品行为以本节和 schema v3 为准。

- [x] 按 `R` 进入一次性矩形创建模式；成功创建一个框后自动回到选择/编辑模式。
- [x] 同一个一次性矩形状态机同时支持左键拖拽释放，以及“第一次单击角点、第二次单击对角点”；两种手势生成完全一致的规范化像素坐标、历史节点和自动保存请求。
- [x] 第一次单击后显示锚点、动态轮廓、实时宽高和辅助线；第二点位于任意方向时都能正确规范化矩形。
- [x] 两次单击是两个独立点击事件；不会误触发已有矩形的双击改标签，也不会因系统双击间隔而提前完成。
- [x] 第二点形成零面积时不生成 shape、历史节点、保存任务或复核变化，并允许重新指定第二点；`Esc` 可无副作用取消锚点。
- [x] `Esc` 取消未完成的矩形并回到选择/编辑模式，不产生标注、历史节点、保存任务或复核状态变化。
- [x] A0.4 历史行为：指针在实际图片区域内时显示水平和竖直辅助线，离开图片、没有图片或中键平移时隐藏，且辅助线不越过图片边界；该规则已由下方已完成的 A0.5 最新目标取代。
- [x] 矩形外框可见且不透明度高于内部填充；填充不遮挡关键图片细节，各标签继续使用自己的颜色。
- [x] 鼠标中键按下拖拽可平移图片，松开后停止，且不改变当前选择或矩形工具状态。
- [x] 普通滚轮只竖直滚动图片，`Alt + 滚轮` 只水平滚动图片，边界处正确停止。
- [x] A0.4 历史行为：工具栏按钮、缩放比例、`100%` 和适配窗口均可用，当时 `Ctrl + 滚轮` 未绑定 DatumDock 操作；该决定已由 A0.8 的最新缩放要求取代。
- [x] 新交互不会破坏框选择、移动、八点缩放、自动保存、撤销/重做、图片切换和双数据集隔离。
- [x] pytest-qt 覆盖一次性状态转换、两种创建手势、辅助线显隐、中键平移及两种滚轮方向；双语和目标分辨率截图通过。
- [x] 双击画布矩形打开“更改标签”小窗并预选当前标签；打开、搜索和浏览候选不会提前修改矩形或触发保存。
- [x] 小窗可从边缘和四角调整尺寸；标签卡片按可用宽度自动增减列数并换行，内容过多时只有中部标签区纵向滚动，顶部搜索和底部操作保持可见。
- [x] 标签卡片显示颜色、中文别名、英文训练名及可到达的描述；当前项和候选项不只依赖颜色区分。
- [x] 搜索可按别名、训练名、同义词和描述即时过滤；无结果状态、`Ctrl+F`、方向键、`Enter` 和 `Esc` 均可用。
- [x] 小窗可快速新建真实数据集标签，复用完整标签管理的字段与唯一性规则；新标签创建后立即出现在候选区。
- [x] 只有点击确认才产生一次标签改派、一个撤销历史节点和一次自动保存；取消或关闭时矩形标签和复核状态保持不变。
- [x] 弹窗尺寸作为全局偏好跨会话记忆；新建标签成功后取消外层改派仍保留该标签，两个事务各自通过回归。
- [x] 单击右侧当前标注列表项后，列表项与画布框同步选中；列表或画布具有操作焦点时按 `Delete` 均删除同一条矩形标注。
- [x] 右侧列表触发的删除只移除当前图片的标注实例，不删除数据集标签定义，也不影响其他图片中的同类标注。
- [x] 列表删除只产生一个历史节点和一次自动保存；画布、列表、框数、反向索引和复核状态同步刷新，`Ctrl+Z` 可完整恢复。
- [x] 搜索框、文本框、快速标签小窗或标签编辑表单具有文本焦点时，`Delete` 不会穿透并误删标注。
- [x] 待复核图片删除最后一个矩形属于有效人工编辑；保存成功后自动变为已完成并以零框表达无目标，保存失败时仍为待复核。
- [x] 图片可通过按钮和比例输入放大到 `6400%`，并可一键恢复 `100%` 或适配窗口；非法和越界比例被安全钳制。
- [x] 高倍率缩放只改变视图变换，不创建与放大尺寸等大的图片副本；原图只保留单份像素缓存。
- [x] 在 `100%`、`800%`、`3200%` 和 `6400%` 下，矩形坐标、鼠标命中、八点控制柄、十字辅助线和保存后的 LabelMe 像素坐标无可观察漂移。

### A0.9 最近使用标签跟随（已完成）

- [x] 在同一数据集内先用 `test1` 创建矩形，再改用 `test2` 成功创建或确认改派后，下一次进入矩形创建必须默认选中 `test2`。
- [x] 最近使用标签跨当前数据集的图片保持，但两个数据集各自独立；快速切换数据集不会串用标签 ID 或显示名称。
- [x] 打开后取消标签小窗、仅搜索/浏览、零面积矩形、`Esc` 取消、只读 shape 和保存失败均不改变最近使用标签。
- [x] 最近标签归档、删除或不属于当前数据集后立即失效，并要求重新选择活动标签，不静默使用标签表第一项代替。
- [x] 最近标签变化本身不创建撤销历史、不单独写 LabelMe JSON，也不改变图片复核状态；pytest-qt 覆盖真实外部矩形改派、保存、下一框跟随和数据集隔离。

### A0.10 启动默认最大化（已完成）

- [x] `python -m datumdock` 与 `python -m datumdock --ui-preview` 的主窗口首次可见时已经最大化，不先闪现普通尺寸窗口。
- [x] 最大化范围是当前单个显示器的可用工作区；保留 Windows 原生标题栏、最小化/还原/关闭按钮和任务栏，不进入无边框独占全屏。
- [x] 用户还原并调整窗口后仍可正常使用；关闭并重新启动时再次按产品默认最大化。
- [x] 新建数据集、标签选择、导入导出、设置和危险确认等对话框保持内容适配尺寸，并完整位于当前显示器可用区域内。
- [x] 对 1366×768、1440×900、1920×1080 的逻辑工作区及负坐标副屏建立几何回归；Qt 逻辑坐标保证 100%、125%、150% DPI 下不跨屏拉伸。
- [x] `6400%` 下仍能通过中键、滚轮和 `Alt + 滚轮` 到达图片四边与四角，框线、控制柄和标签不会随倍率无限放大。
- [x] 800% 以下采用平滑插值，800% 及以上采用像素清晰模式；当前最大倍率锁定为 `6400%` 并有截图与坐标测试。

### A0.5 十字辅助线持续跟随整改（已完成）

> 本节是 2026-07-19 在 A0.4 验收后新增的规则，优先于 A0.4 中“中键平移时隐藏”的历史行为；2026-07-20 已取得实现、测试和截图证据。

- [x] 鼠标进入实际图片区域后，无需选择矩形工具或开始画框，水平和竖直辅助线立即出现并持续跟随指针。
- [x] 选择/编辑、矩形创建、框移动、八点缩放、中键平移以及其他画布操作期间，辅助线都保持显示并随指针移动。
- [x] 中键平移导致图片位置变化时，辅助线按当前指针与移动后图片区域实时重新裁切，不冻结在旧图片坐标，也不延伸到图片外。
- [x] 只有指针离开实际图片、空画布或图片加载失败时隐藏辅助线；中键按下、非矩形工具和没有草稿都不是隐藏条件。
- [x] 辅助线移动属于纯视图反馈，不创建历史节点、不触发自动保存、不改变复核状态，也不抢占选择、拖框、移动/缩放框或平移输入。
- [x] 在 `100%`、`800%`、`3200%`、`6400%` 以及滚动到图片四边角时，辅助线仍通过统一双精度坐标变换准确落在指针对应的原图像素上。
- [x] pytest-qt 覆盖选择模式、矩形待命/首点锚定、框编辑、中键平移、图片内外切换和空/失败画布；双语目标尺寸截图证明非画框状态也能看到辅助线。

### A0.6 标注移动与缩放鼠标指针样式整改（已完成）

> 本节的“鼠标指针样式”专指 Windows/Qt 的实际系统鼠标指针图标，不是标注框、标签颜色、控制点或辅助线样式；2026-07-20 已通过 pytest-qt、DPI 子进程与 Win32 原生截图复验。

- [x] 选择/编辑模式下，系统鼠标指针进入可编辑矩形框内部的移动区域时显示四向移动样式，开始拖动后保持到释放或取消。
- [x] 左/右控制点显示水平缩放指针，上/下控制点显示垂直缩放指针；左上/右下和右上/左下分别显示两种对应的对角缩放指针。
- [x] 鼠标指针方向与屏幕上的实际缩放方向一致，反向拖动、框翻转规范化和 `100% / 800% / 3200% / 6400%` 下都不会显示错误方向。
- [x] 指针样式优先级为中键平移、矩形创建、缩放控制点、框内移动、普通选择；重叠命中时只显示最高优先级操作的指针。
- [x] 鼠标释放、`Esc`、切换工具/图片/数据集、删除当前框、加载失败或失去画布焦点后，系统指针立即恢复当前上下文，不残留移动或缩放样式。
- [x] 只读兼容 shape、损坏或恢复锁定的标注文档，以及其他不可编辑对象不显示移动/缩放指针。
- [x] 仅改变鼠标指针图标不会创建撤销历史、自动保存请求或复核状态变化，也不影响 A0.5 十字辅助线持续跟随。
- [x] pytest-qt 直接断言框内、八个控制点、拖动中、释放、取消、工具优先级、只读对象和高倍率下的 Qt 鼠标指针类型；Win32 原生屏幕捕获另记录移动、水平/垂直、两类对角、矩形和闭合手形指针及真实 HCURSOR。

### A0.7 浅色底板与图片边缘吸附画框（已完成）

> 本节取代步骤四早期“大面积深色画布”的视觉规则，并扩展矩形创建的输入范围；最终标注数据仍完全位于图片边界内，且 Service 保留独立校验。

- [x] 图片后方的中央画布使用集中式冷调浅色 `canvasBackplate`，浅色图片通过 `canvasImageBoundary` 细边界清楚区分；不保留大面积深色底板或厚阴影。
- [x] 拖拽创建的起点或终点落在中央底板时，分别钳制到最近图片边或角；动态预览和最终矩形边缘精确贴合图片边界，不直接取消本次创建。
- [x] 两次单击创建对两个点执行与拖拽完全相同的钳制；图片内点不改变，左右/上下/四角外点逐轴钳制，反向绘制后正确规范化。
- [x] 图片内起点拖到图片外、底板起点拖入图片、相对两侧底板间拖拽和四个角外输入均有明确回归；两个点钳制后零面积时不创建虚假最小框，并允许重新指定有效点。
- [x] 底板吸附只在矩形工具、图片已加载、文档可编辑且存在活动标签时生效；选择工具点击底板不画框，中键平移优先，中央画布以外的控件不参与吸附。
- [x] A0.5 辅助线继续裁切在实际图片内；指针位于底板时不把十字线延伸到底板，使用空心吸附点和短边缘刻度反馈当前钳制位置。
- [x] `100% / 800% / 3200% / 6400%`、适配窗口、平移和滚动后都使用同一双精度变换与钳制函数，最终 LabelMe 坐标不越界且不产生倍率相关漂移。
- [x] Service/Repository 故障与直接越界输入测试证明后端仍拒绝越界框；UI 吸附不会放宽领域校验、绕过自动保存事务或改变零面积规则。
- [x] pytest-qt 覆盖拖拽/两点、四边/四角、零面积、工具优先级和无图片/只读状态；双语三分辨率截图更新为浅色底板并展示贴边矩形与吸附反馈。

### A0.8 用户实机反馈整改（已完成）

> 本节由 2026-07-20 用户对普通模式的实际操作反馈触发。最终品牌尺寸按随后反馈调整为与首页相同的 170px 可见宽度基准，并将返回主页行为从 Logo 分离为左侧独立按钮。

- [x] 1366×768、1440×900 和 1920×1080 常规工作台左上角显示与首页同一视觉尺寸的完整 `DD + DatumDock` 字标，非透明可见内容以 170 逻辑像素为基准、保持在 160～180 逻辑像素，高度不低于 24 逻辑像素。
- [x] 完整字标为不可点击的静态展示；左侧独立返回按钮安全返回主页并具有双语 tooltip/无障碍名称。品牌区不会挤压数据集切换、导入、导出等主要操作，只有真实空间不足时才收敛为 `DD`。
- [x] 普通模式刚进入工作台且默认选择工具生效时，无需选择矩形工具、无需选中已有框，鼠标进入图片后两条辅助线立即出现并持续跟随。
- [x] 一次性画框成功并自动退回选择模式、清除当前选择、切换图片或切换数据集后，辅助线规则保持一致；只有离开图片、空画布、加载中或加载失败时隐藏。
- [x] 普通模式和 `--ui-preview` 使用同一辅助线可见性规则；正式受管工作台路径和共享预览画布均有真实事件证据。
- [x] `Ctrl + 滚轮` 向上放大、向下缩小，并在指针位于图片内时尽量保持指针下的原图像素停留在同一屏幕位置；指针不在图片内时以可视区域中心缩放。
- [x] 滚轮修饰键优先级为 `Ctrl` 缩放、`Alt` 横向滚动、无修饰键纵向滚动；`Ctrl + Alt + 滚轮` 按缩放处理，不同时横向滚动。
- [x] `Ctrl + 滚轮` 与按钮、比例输入和键盘缩放动作共享 1%～6400% 视图状态；到达上下限后安全停止，不创建巨型图片副本。
- [x] Logo、返回按钮、辅助线和 `Ctrl + 滚轮` 仅改变导航、展示或视图，不创建标注历史、不触发自动保存、不改变复核状态，也不修改真实资料库。
- [x] pytest-qt 使用真实鼠标移动和 `QWheelEvent` 覆盖默认选择、无选中框、Ctrl/Alt 优先级、指针锚点和高倍率坐标；普通模式 GUI 冒烟实际经过正式工作台装配路径。
- [x] 中文/英文 × 三种目标分辨率 × 100%/125%/150% DPI 共 36 张结构化截图，以及 12 张 Windows 原生截图，证明品牌尺寸、独立返回按钮、默认选择模式辅助线和缩放前后状态。
- [x] Python 3.11 完整质量门、普通/预览隔离和真实 `%LOCALAPPDATA%\DatumDock` 哈希复验通过。

## A. 启动与界面

- [ ] 在目标平台执行项目说明中的启动命令后，应用正常打开。
- [ ] 标注工作台包含顶部标题与主操作栏、左侧标注工具栏、中央图像画布、右侧标注/图片分区和底部状态栏。
- [ ] 顶部始终显示当前数据集名称，并可执行导入图片、打开明确分类的导出入口和快速切换数据集；切换不会打开外部目录选择器。
- [ ] 当前图片保存失败时，快速切换数据集会被阻止，并提供重试、放弃内存修改或取消。
- [ ] 左侧工具栏至少可到达选择/编辑、矩形框、AI 标注、平移、适配窗口、撤销和重做；激活、悬停和键盘焦点状态清晰。
- [ ] 右侧上半区显示当前图片标注，下半区显示图片列表/网格，两区可调整高度和折叠；右侧刷新不会造成画布明显跳动。
- [ ] 首次启动主页及空数据集工作台均有明确的引导式空状态提示。
- [ ] 界面遵守 `docs/VISUAL_DESIGN.md` 的现代视觉 v2，不再出现默认 Qt 灰色、暖灰/灰绿旧式管理软件、立体按钮、凹陷输入框或密集工具栏分隔线。
- [ ] 主页使用冷白/浅蓝灰背景、清晰品牌蓝、宽松卡片和友好图标；标注页使用紧凑工具、白色信息面板和冷调浅色画布底板，两者组件语言一致但密度不同。步骤四早期深色画布只作为历史截图证据。
- [ ] 首次打开 5 秒内能识别数据集、新建和学习入口；进入标注页 5 秒内能识别当前数据集、导入/导出、矩形框、AI、画布、当前标注和图片状态列表。
- [ ] 主页参考 Scratch 的亲和与引导、标注页参考 X-AnyLabeling 的效率与信息密度，但发布包不包含其品牌、图标、截图、代码或像素级复制布局。
- [ ] 主按钮、次按钮、工具按钮、危险按钮、hover、active、disabled 和键盘焦点使用一致组件规则；页面不靠高饱和彩虹色或厚阴影制造现代感。
- [ ] 主窗口、数据集网格/列表、标签表、表单、导出向导和确认对话框均复用同一组设计令牌、间距和文字层级；数据集标签颜色不替代系统成功、警告、错误或选中状态。
- [ ] 在默认系统缩放与常用高 DPI 缩放下，文字、图标、状态角标和点击目标不重叠、不被裁切；键盘焦点、加载、未保存、错误和禁用状态均不只依赖颜色表达。
- [ ] 核心导航、导入导出、标注画布、模型、设置、删除/回收站和状态图标均使用项目自有且风格统一的资产；仅图标按钮具有 tooltip 和可访问名称，发布包中的应用图标在开始菜单和安装目录显示正常。
- [ ] 设置页支持简体中文与英文，默认简体中文；切换后当前可见系统界面文案立即更新，无需重启。
- [ ] 切换界面语言不会改变数据集标签、描述、数据集名、文件名、标注内容或模型元数据。
- [ ] 设置页从统一操作注册表展示并搜索全部 DatumDock 应用快捷键，而不是仅列出常用操作；自动化测试证明不存在绕过注册表的页面级硬编码应用快捷键。
- [ ] 用户可修改或清空任一应用快捷键；修改后无需重启即可在全部已打开窗口、菜单、tooltip 和弹窗中生效，清空后操作仍有可见 UI 入口。
- [ ] 设置页会阻止冲突和系统保留组合，或在用户确认替换时明确取消旧绑定。
- [ ] 用户可恢复单项、分组或全部默认快捷键；“恢复全部默认按键”显示影响数量，确认后一次恢复所有分组的当前出厂绑定。
- [ ] 恢复全部默认按键经过原子持久化并在重启后保持；故障注入时不会只恢复一部分，也不会修改语言、显示、数据管理设置或任何数据集文件。
- [ ] 后续新增的带键盘入口操作会因注册表枚举自动出现在设置页，并纳入默认值、冲突和恢复全部测试。
- [ ] 新安装或无用户覆盖时，标注工作台按 `A` 打开上一张图片、按 `D` 打开下一张图片；设置页显示相同出厂默认值。
- [ ] 修改 `A / D` 绑定后旧按键立即失效、新按键立即生效；执行恢复全部默认按键后重新恢复 `A / D`，并在重启后保持。
- [ ] 搜索框、标签编辑和其他文本输入获得焦点时，输入 `a` 或 `d` 不切换图片；返回画布浏览上下文后快捷键恢复生效。
- [ ] 使用 `A / D` 切图仍等待最新自动保存；保存失败时不离开当前图片，并显示既定的重试/放弃/取消保护。
- [ ] 首张按 `A`、末张按 `D` 不会产生无效索引、跨数据集跳转或重复保存，并显示轻量边界反馈。
- [ ] 新状态 UI 只提供“待复核 / 已完成”两种图片级复核状态，不显示未复核、待审核、有问题或已完成（无目标）等旧状态入口。
- [ ] 纯人工创建首个矩形后图片默认已完成；模型新增或改变标注后图片为待复核，即使此前已经完成。
- [ ] 待复核图片发生创建、移动、缩放、补框、删除、换标签或实际改变文档的撤销/重做后，在第一次有效编辑保存成功时自动变为已完成。
- [ ] 选择框、缩放/平移视图、搜索、筛选、打开后取消弹窗等只读操作不会把待复核误改为已完成。
- [ ] 人工编辑内容与自动完成状态在同一原子保存中提交；故障注入时不能出现框已保存但状态仍待复核，或状态完成但框未保存。
- [ ] 检查后无需修改时，可点击“确认已完成”或使用其可配置快捷键；保存失败时状态保持待复核且可重试，操作不修改矩形。
- [ ] 新导入无标注图片不显示第三种复核徽标，可通过“无标注”条件筛选；损坏/恢复失败使用独立健康诊断。
- [ ] 零框图片经人工明确确认后使用普通“已完成”，可作为负样本；不创建 `completed_negative` 用户状态。
- [ ] SQLite v2→v3 在事务中将旧状态安全映射并可回滚：未复核→空值、待审核→待复核、完成/无目标完成→完成、有问题→待复核。

## B. 数据集池与图片浏览

- [ ] 用户可从主页创建至少两个独立数据集，不需要创建或打开工作区和项目。
- [ ] 用户新建数据集时可选择已有数据集作为模板，复用兼容标签集、命名规则和导入规范，但不复制图片或标注。
- [ ] 用户可返回主页并快速打开其他数据集；切换后只显示目标数据集的样本、类别、模型与临时导出上下文。
- [ ] 用户可导入至少一个图片文件夹到当前数据集的受管数据集池。
- [ ] 用户可导入一个 X-AnyLabeling 标注目录；每张已配对图片及同名 LabelMe JSON 均进入受管池，矩形框可在 DatumDock 中继续编辑，外部源目录不被改写。
- [ ] 导入含多边形、旋转框、圆、线、点或 shape 扩展字段的 X-AnyLabeling JSON 时，应用明确显示当前不可编辑的内容已保留；保存、重命名和标签名称迁移后，这些内容不被静默删除。
- [ ] 可导入 JPG/JPEG、PNG、BMP、WebP、TIFF 等常见静态图片；MVP 统一转换为受管 PNG，外部源文件不被修改。
- [ ] 检测到完全相同图片时，导入对话框显示待导入与已有图片的对比和来源；用户可取消或明确继续并同时保留两份样本。
- [ ] 软件能列出支持的图片文件，显示其基本标注状态，并在重复导入时给出提示。
- [ ] 导入后的图片和标注位于目标数据集池中；不同数据集的池内容互不混用。
- [ ] 用户可配置当前数据集的命名规则，并在执行前看到全部旧名称、新名称和冲突的预览。
- [ ] 批量重命名后，图片、配套 LabelMe JSON、`imagePath`、索引和缩略图引用均保持一致，且样本稳定 ID 不变。
- [ ] 用户可从标注页面删除当前图片；确认后该样本的受管图片、标注、索引和派生信息全部被清理，其他样本、数据集标签和外部导入来源文件不受影响。
- [ ] 少量样本删除可移入软件回收站并恢复；“少量”阈值可在全局设置修改并有悬停说明；大批量样本可在明确确认后永久删除，两个操作都显示影响范围。
- [ ] 单个数据集导入至少一万张常见静态图片后，可分页浏览、搜索、筛选、打开图片、执行标签检查与导出，且界面不会因全量样本或缩略图加载而失去响应。
- [ ] 网格视图的标注预览开关可显示或隐藏缩略图矩形框与标签色；关闭时仍清晰显示原图和图片级状态。
- [ ] 右侧图片列表的每一行显示缩略图、文件名、标注数量，并在行末固定显示“文字 + 图标/形状”的图片级状态徽标。
- [ ] 图片级复核状态只包括待复核和已完成；无标注、健康异常和零框分别通过数据条件、诊断和框数表达。
- [ ] 没有目标框的负样本可被人工确认成普通“已完成”，并能通过零框条件与尚未处理的无标注图片区分。
- [ ] AI 标注结果自动进入待复核；人工完整检查并确认后切换为已完成。
- [ ] 能正确打开 PNG、JPG/JPEG 至少两种常见格式。
- [ ] 用户可通过 UI 和快捷键切换上一张、下一张图片。
- [ ] 图片可缩放、平移，且缩放后标注位置仍正确。

## C. 标注编辑

- [ ] 用户可为数据集创建标签集；每个标签可配置英文训练名、中文别名、描述、同义词、稳定类别 ID 和颜色。
- [ ] 每个活动标签自动或手动获得数据集内唯一、可辨识的颜色；应用拒绝活动标签之间的颜色重复。
- [ ] 用户可通过独立标签管理页面集中查看、搜索、筛选、添加、编辑和归档当前数据集全部标签；页面顶部表格至少展示英文训练名、中文别名、描述、类别 ID、状态和使用次数。
- [ ] 从已有数据集复制配置后，新数据集拥有独立标签定义，同时保留兼容的稳定标签 ID、类别 ID 和英文训练名映射。
- [ ] 标签选择器默认展示中文别名、英文训练名和简短描述，并可按别名、英文名、同义词和描述搜索。
- [ ] 同一标签的颜色在所有数据集的画布、标签表、标签选择器、筛选/检查集合和自动标注建议中一致；关键状态不只依赖颜色区分。
- [ ] 用户可在标签管理表中对任一标签打开当前数据集的标签检查集合，查看所有包含该标签的图片和目标标签框数量。
- [ ] 用户可从检查集合跳转至原始标注画布，且目标标签框高亮显示；检查集合不复制或移动任何图片和标注文件。
- [ ] 用户可在图片上创建矩形框，并关联到指定标签。
- [ ] 修改中文别名、描述、同义词或颜色后，所有数据集中的标签显示即时更新。
- [ ] 修改英文训练名时，软件在写入前展示受影响范围；确认后当前数据集标签集和所有受影响 LabelMe 标注文件同步更新。
- [ ] 迁移中发生写入失败时，软件能报告失败文件并保持或恢复一致状态，不能静默只更新部分标注。
- [ ] 用户可选中、移动、缩放和删除矩形框。
- [ ] 点击右侧当前图片标注项会选中并定位对应画布框；点击画布框也会同步选中右侧标注项。
- [ ] 双击画布框或右侧标注项可快速切换标签，修改后立即自动保存。
- [ ] 编辑操作会立即原子保存并显示保存中/已保存状态；写入失败时显示可处理的待保存错误状态。

## D. 标注保存与读取

- [ ] 自动保存会为图片生成或更新同名 LabelMe JSON；用户可用 `Ctrl+S` 重试失败保存。
- [ ] 重新打开图片后，已保存的矩形框、类别与坐标保持一致。
- [ ] 损坏 JSON 不会使应用崩溃，也不会在未确认时被覆盖。
- [ ] 自动保存失败后离开图片时，用户可选择重试保存、放弃内存修改或取消。

## E. 自动标注模型库

- [ ] 每个数据集具有隔离的受管模型目录和独立模型管理页面；用户可查看、导入、更新和删除当前数据集模型。
- [ ] 导入 ONNX 后，应用能展示探测到的输入输出、尺寸、可用类别和运行配置，且只有本地校验通过后模型才可用于自动标注。
- [ ] 导入受支持 PT 变体后，应用能识别并展示其配置；无法识别或无法安全运行的 PT 文件必须显示明确的不支持原因。
- [ ] 可导入并运行 Ultralytics YOLO 检测模型及兼容自训练 YOLO 模型；其他不兼容 PT 文件不会被猜测运行。
- [ ] 用户可查看并修改模型类别到当前数据集标签的映射；未映射模型类别不会创建标注。
- [ ] 更新模型时，旧模型在新模型验证成功前保持可用；删除模型不删除图片、数据集或人工标注。
- [ ] 有可用 GPU 推理后端时自动标注优先使用 GPU；首次 CPU 推理时说明原因、允许继续，并提供应用内 GPU 配置指引。
- [ ] 自动标注结果直接保存，带模型来源、置信度和图片级待复核状态；用户有效修改漏标、误标、标签或框位置并保存成功后自动完成，无修改时可明确确认，且自动标注不会静默覆盖人工标注。
- [ ] 自动标注改变已完成图片时重新进入待复核；随后第一次有效人工编辑保存成功会自动完成，无修改检查则通过按钮或可配置快捷键完成。

## F. 模型格式导出

- [ ] 用户可在全局设置设定默认 train/val/test 比例，并可在每次导出时选择当前数据集池样本范围、目标模型/格式、临时覆盖比例、随机种子和输出目录。
- [ ] 用户完成或取消导出后，应用不保存导出方案、导出路径或导出历史。
- [ ] 软件阻止比例和不为 100% 的导出，并对空划分给出清晰提示。
- [ ] 在输入样本和种子不变时，重复导出得到相同的样本划分。
- [ ] 当完全重复图片被划分到不同训练/验证/测试集合时，导出前会显示数据泄露警告。
- [ ] 已确认近似图片组中的所有样本在导出时被强制分配到同一个 train/val/test 集合；若组约束使比例偏差，导出预览会解释原因。
- [ ] MVP 的 YOLO Detection 导出目录包含 `images/train`、`images/val`、`images/test`、对应 `labels` 子目录与 `data.yaml`。
- [ ] 每个 YOLO 标签行符合 `class_id x_center y_center width height`，且后四项处于 `0..1`；`class_id` 与当前数据集标签集映射一致。
- [ ] 导出不会修改或移动数据集池中的原图与原标注。

## F.1 X-AnyLabeling 双向互操作

- [ ] 导入预检同时读取 JSON `shape.label` 与根目录 `labels.txt`；未知标签默认新建为当前数据集标签，现有同名活动标签自动映射，用户仍可显式改为已有标签或只读保留。
- [ ] 新建映射对引用数为零的预定义标签有效；中文、空格等外部显示名保留为别名并获得合法唯一训练名，导入矩形按稳定标签 ID 成为可编辑标注。
- [ ] 标签映射表在中英文和目标分辨率下无裁切、重叠或不可辨识选择；导入报告展示新建标签数与文件级失败原因。
- [x] 用户可从当前数据集导出一个独立 X-AnyLabeling 目录，其中含范围内全部 PNG、仅有 shape 图片的同名 `.json` 和 `labels.txt`；完全无 shape 图片不生成空 JSON，只含兼容 shape 的图片仍生成 JSON。每个已生成 JSON 的 `imagePath` 指向同目录图片，尺寸与图片实际尺寸一致。
- [x] DatumDock 产生的矩形框以 LabelMe `shape_type: "rectangle"`、英文训练标签名和两个对角点写入；DatumDock 私有的样本 ID、复核状态、模型来源和数据集元数据不写入交换 JSON。
- [x] 自动化回归证明导入后未经编辑再导出，以及 DatumDock 编辑矩形后再导出，均保留未支持 shape 和兼容字段；冲突或解析失败产生文件级报告且不发布不完整目录。训练名迁移已有步骤四独立回归覆盖。
- [ ] 自动化测试覆盖 X-AnyLabeling 样例目录的导入、矩形编辑、未支持 shape 保留及再导出；人工回归用已安装的 X-AnyLabeling 打开导出目录并验证图片、标签和矩形框可见。

> 2026-07-20 复验说明：上述最后一项的自动化部分和 100 图 DatumDock 闭环已通过；人工 X-AnyLabeling v3.3.10 打开/编辑/回导因可信依赖源 TLS 中断尚未完成，因此 F.1 和步骤五整体仍未通过硬性验收。

## G. 质量

- [ ] 标注模型、LabelMe 序列化、数据集划分与 YOLO 导出逻辑具有自动化测试。
- [ ] 测试可重复运行并通过。
- [ ] 能完成一个包含至少 100 张普通图片的数据集浏览与逐张保存流程，无应用崩溃。

## H. GUI 与安装包交付

- [ ] 创建数据集、导入数据、标注、筛选、导出等核心流程均可仅使用 GUI 完成。
- [ ] 发布构建可生成 Windows 安装包，且安装、卸载和开始菜单快捷方式均正常工作。
- [ ] 在未安装 Python 的干净 Windows 环境中，用户可安装并启动应用。
- [ ] 该环境中可以完成一次图片导入和 YOLO 导出，不依赖开发目录或终端。

## I. 数据集备份与数据集转移

- [ ] 用户可导出数据集压缩备份包；导入前应用校验格式、清单和完整性，校验失败不创建半导入数据集。
- [ ] 成功导入备份后，数据集池、标签集、标注和索引可继续使用；模型配置保留，但模型二进制明确标记为需单独重新导入。
- [ ] 用户只能在标签集完全一致的数据集之间批量复制或移动样本；不一致时应用明确阻止并说明原因。
- [ ] 用户可比较两个标签集的训练映射和显示信息；只有无训练映射冲突时才可合并标签集。
- [ ] 用户可合并标签兼容的数据集；合并前看到标签签名、样本数和重复图片预览，合并后不丢失标注或留下半移动样本。

## J. X-AnyLabeling 对标发布闸门

- [ ] 发布说明明确当前完成等级（L1/L2/L3/L4）、已支持形状、格式、模型类型和已知限制。
- [ ] 声称“核心标注工作流可用”前，已完成 `docs/X_ANYLABELING_BASELINE.md` 的 L2 必需项。
- [ ] 声称“AI 与多格式工作流可用”前，已完成该基线的 L3 必需项。
- [ ] 未支持的功能不会以可点击但无效的入口、错误格式导出或静默降级的方式呈现。

## 验收记录模板

| 日期 | 阶段 | 验证人 | 结果 | 说明 |
| --- | --- | --- | --- | --- |
| | | | | |

## English Summary

Acceptance now includes pending gates for safeguarded dataset deletion, X-AnyLabeling label import, recent-label tracking, and maximized startup. The startup gate requires the main window to appear maximized on one active display while retaining native Windows controls and taskbar, with dialogs remaining content-sized.
